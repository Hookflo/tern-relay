{
  "name": "tern-relay",
  "version": "1.0.0",
  "private": true,
  "description": "Cloudflare Worker WebSocket relay for @hookflo/tern-dev. Self-hostable.",
  "license": "MIT",
  "scripts": {
    "dev":    "wrangler dev",
    "deploy": "wrangler deploy",
    "types":  "wrangler types",
    "check":  "tsc --noEmit"
  },
  "devDependencies": {
    "wrangler":                   "^3.0.0",
    "@cloudflare/workers-types":  "^4.0.0",
    "typescript":                 "^5.4.0"
  }
}
